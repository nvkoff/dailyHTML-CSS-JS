{
  "id": "js/if-else",
  "track": "js",
  "unit": "Control flow",
  "order": 9,
  "title": "if / else if / else",
  "description": "Branching your code.",
  "xp": 10,
  "questions": [
    {
      "type": "predict",
      "prompt": "What logs when x = 5?",
      "language": "js",
      "code": "const x = 5;\nif (x < 0) console.log('neg');\nelse if (x === 0) console.log('zero');\nelse console.log('pos');",
      "options": ["neg", "zero", "pos", "nothing"],
      "answer": 2,
      "explanation": "5 is not < 0, not === 0, so it falls to else → 'pos'."
    },
    {
      "type": "mcq",
      "prompt": "Which is BAD practice?",
      "options": [
        "Wrapping single-line if bodies in { }",
        "Chaining else if",
        "Assigning inside an if condition (if (x = 5))",
        "Testing typeof to guard"
      ],
      "answer": 2,
      "explanation": "if (x = 5) assigns 5 to x and evaluates truthy — a silent bug. Use === for comparison."
    },
    {
      "type": "fix-bug",
      "prompt": "This should log 'adult' only when age is 18 or more. Fix it.",
      "language": "js",
      "broken": "const age = 17;\nif (age = 18) console.log('adult');",
      "mustInclude": [">="],
      "hint": "Use >= for comparison, not = which is assignment.",
      "explanation": "Comparison operator, not assignment. `age >= 18` is what you want."
    }
  ]
}
