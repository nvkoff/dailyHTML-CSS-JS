{
  "id": "js/switch",
  "track": "js",
  "unit": "Control flow",
  "order": 10,
  "title": "switch",
  "description": "Branch on a value — but watch fall-through.",
  "xp": 10,
  "questions": [
    {
      "type": "predict",
      "prompt": "What logs?",
      "language": "js",
      "code": "const x = 2;\nswitch (x) {\n  case 1: console.log('a');\n  case 2: console.log('b');\n  case 3: console.log('c');\n  default: console.log('d');\n}",
      "options": ["b", "b c d", "d", "TypeError"],
      "answer": 1,
      "explanation": "Without break, execution falls through all subsequent cases."
    },
    {
      "type": "mcq",
      "prompt": "How does switch compare each case to the value?",
      "options": ["Uses ==", "Uses ===", "Uses equals()", "String coercion"],
      "answer": 1,
      "explanation": "switch uses strict equality (===)."
    },
    {
      "type": "fix-bug",
      "prompt": "Make this only log 'yes' when status === 'ok'.",
      "language": "js",
      "broken": "switch (status) {\n  case 'ok': console.log('yes');\n  case 'error': console.log('err');\n}",
      "mustInclude": ["break"],
      "explanation": "Add `break;` after the 'yes' log to stop fall-through."
    }
  ]
}
