{
  "id": "js/map-filter",
  "track": "js",
  "unit": "Arrays",
  "order": 19,
  "title": "map and filter",
  "description": "Transform and keep — without mutating the source.",
  "xp": 12,
  "questions": [
    {
      "type": "predict",
      "prompt": "What logs?",
      "language": "js",
      "code": "const nums = [1, 2, 3, 4];\nconst evens = nums.filter(n => n % 2 === 0);\nconsole.log(evens);",
      "options": ["[1, 3]", "[2, 4]", "[true, false]", "[]"],
      "answer": 1,
      "explanation": "filter keeps elements where the callback returns truthy."
    },
    {
      "type": "predict",
      "prompt": "What logs?",
      "language": "js",
      "code": "const words = ['hi', 'hey'];\nconst lens = words.map(w => w.length);\nconsole.log(lens);",
      "options": ["['hi', 'hey']", "[2, 3]", "5", "['h', 'h']"],
      "answer": 1,
      "explanation": "map returns a new array of transformed values — same length as source."
    },
    {
      "type": "mcq",
      "prompt": "map and filter:",
      "options": [
        "Mutate the original array",
        "Return a new array; leave the source alone",
        "Return the same array",
        "Return a promise"
      ],
      "answer": 1,
      "explanation": "Both return new arrays; the original stays intact."
    }
  ]
}
